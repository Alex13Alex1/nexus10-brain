# 🐝 NEXUS v1.1 SWARM HIERARCHICAL — Руководство пользователя

## Что это такое?

**NEXUS** — это автономная AI-фабрика с динамическим созданием специалистов. Вы описываете идею — **SwarmEngine** анализирует задачу, создает нужных экспертов и генерирует:

- ✅ Исследование технологий
- ✅ Архитектуру проекта  
- ✅ Рабочий код (Python)
- ✅ Тесты и QA отчёт
- ✅ Оптимизацию производительности
- ✅ Аудит безопасности + Kill Switch
- ✅ Docker-контейнер для деплоя

---

## 🚀 Быстрый старт (5 шагов)

### 1. Запуск системы

Откройте терминал в папке `brain` и выполните:

```powershell
.\venv\Scripts\Activate.ps1
python -m streamlit run app.py
```

Браузер автоматически откроется на `http://localhost:8530`

---

### 2. Интерфейс системы

```
┌─────────────────────────────────────────────────────────────────┐
│  NEXUS v0.9 Security Edition                                    │
├─────────────┬───────────────────────────────────────────────────┤
│  SIDEBAR    │   MAIN AREA                                       │
│             │                                                    │
│  Агенты:    │   ⚙️ Параметры проекта                            │
│  🔍 Scout   │   ┌─────────────────────────────────────────────┐ │
│  🏗️ Arch    │   │ [Название проекта]                         │ │
│  👨‍💻 Dev     │   │ [Описание: Что должна делать программа?]   │ │
│  🧪 QA      │   └─────────────────────────────────────────────┘ │
│  🎓 Mentor  │                                                    │
│  🔐 Security│   👁️ Визуальный ввод (скриншот/wireframe)         │
│  🏥 SRE     │                                                    │
│             │   [🚀 ЗАПУСТИТЬ ПРОИЗВОДСТВО]                      │
└─────────────┴───────────────────────────────────────────────────┘
```

---

### 3. Создание проекта

**Шаг A:** Введите название (например: `Secure API`)

**Шаг B:** Опишите задачу максимально подробно:

```
Создай REST API с JWT аутентификацией и rate limiting.
Стек: Flask, Flask-JWT-Extended, Flask-Limiter.
Добавь логирование и обработку ошибок.
```

**Шаг C:** (Опционально) Загрузите скриншот UI или диаграмму

**Шаг D:** Нажмите **🚀 ЗАПУСТИТЬ ПРОИЗВОДСТВО**

---

### 4. Что происходит после запуска?

Система последовательно запускает 7 агентов:

| # | Агент | Что делает | Время |
|---|-------|-----------|-------|
| 1 | 🔍 **Tech Scout** | Исследует лучшие библиотеки 2026 | ~30 сек |
| 2 | 🏗️ **Architect** | Проектирует архитектуру + Mermaid | ~45 сек |
| 3 | 👨‍💻 **Developer** | Пишет код + requirements.txt | ~60 сек |
| 4 | 🧪 **QA Engineer** | Тестирует и находит баги | ~30 сек |
| 5 | 🎓 **Performance Mentor** | Оптимизирует по KPI | ~30 сек |
| 6 | 🔐 **Security Auditor** | Создает Kill Switch + аудит | ~45 сек |
| 7 | 🏥 **SRE Observer** | Dockerfile + docker-compose | ~30 сек |

**Общее время: 4-6 минут** (зависит от сложности)

---

### 5. Где найти результаты?

После завершения файлы появятся в папке:

```
brain/projects/[Название_проекта]/
├── docs/
│   ├── architecture.md      # Архитектурный план
│   ├── research.md          # Исследование технологий
│   └── performance_audit.md # KPI отчёт
├── source_code/
│   ├── main.py              # Основной код
│   ├── requirements.txt     # Зависимости
│   └── security_control.py  # Kill Switch + алерты
├── deployment/
│   ├── Dockerfile           # Контейнеризация
│   └── docker-compose.yml   # Оркестрация
├── reports/
│   └── security_audit.md    # Отчёт безопасности
└── tests/
    └── qa_report.md         # QA отчёт
```

---

## 🔐 Модуль безопасности (Security Edition)

Каждый проект автоматически получает `security_control.py`:

```python
# 🛑 Kill Switch — мгновенная остановка системы
def emergency_stop():
    global SYSTEM_ACTIVE
    SYSTEM_ACTIVE = False
    graceful_shutdown()

# 🔍 Маскировка данных
def mask_sensitive_data(data):
    return data[:2] + '*' * (len(data) - 4) + data[-2:]

# 🚨 Система алертов
def send_alert(severity, message):
    # INFO / WARNING / CRITICAL
    ...
```

---

## 🐳 Деплой через Docker

### Локальный запуск:

```bash
cd projects/[Название_проекта]/deployment
docker-compose up --build -d
```

### Проверка статуса:

```bash
docker ps
docker logs [container_name]
```

### Остановка:

```bash
docker-compose down
```

---

## ⚡ Примеры промптов

### REST API
```
Создай REST API для управления задачами (CRUD).
Стек: FastAPI, SQLite, Pydantic.
Добавь пагинацию и фильтрацию.
```

### Торговый бот
```
Создай криптобота на CCXT с RSI + Bollinger Bands.
Биржа: Binance (testnet).
Добавь стоп-лоссы, логирование в CSV, paper trading режим.
```

### Telegram бот
```
Создай Telegram бота для отслеживания цен криптовалют.
Стек: aiogram 3.0, CoinGecko API.
Команды: /price BTC, /alert BTC 50000.
```

### CLI утилита
```
Создай CLI для шифрования файлов с AES-256.
Стек: Click, cryptography.
Команды: encrypt, decrypt, генерация ключа.
```

---

## 🧠 Memory System

Система запоминает ваши предпочтения:
- **Short-term:** Текущая сессия
- **Long-term:** Паттерны между проектами
- **Entity:** Связи между концептами

Чем больше проектов — тем умнее система!

---

## ⚠️ Возможные проблемы

### Markdown в коде
Если в `main.py` появились ```python — это ошибка агента.
**Решение:** Исправьте вручную или запустите Self-Healing

### UTF-8 ошибки
При русских символах в консоли.
**Решение:** Добавлено автоматически в `sys.stdout.reconfigure(encoding='utf-8')`

### API Key истёк
`401 Unauthorized` ошибка.
**Решение:** Обновите ключ в `.env` файле

---

## 📊 Стоимость

Примерные расходы OpenAI за один проект:

| Модель | Использование | ~Цена |
|--------|--------------|-------|
| GPT-4o | Architect, Dev, Security | ~$0.15 |
| GPT-4o-mini | Research, QA, SRE | ~$0.02 |
| **Итого** | 1 проект | **~$0.17** |

---

## 🆘 Поддержка

- **Логи агентов:** Смотрите в терминале во время выполнения
- **Отладка:** Все файлы сохраняются в `projects/[name]/`
- **Перезапуск:** `Ctrl+C` и снова `streamlit run app.py`

---

**NEXUS v0.9 Security Edition** — ваша персональная фабрика программного обеспечения! 🚀

